# Redis Configuration for ResearchMind
# Add these to your .env file to enable Redis persistence

# Redis Connection
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=  # Optional, uncomment if you set a password

# Redis is optional - the system will fall back to file-based storage if Redis is unavailable
